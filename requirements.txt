# Core dependencies for Knowledge Graph-Guided RL Training

# Deep Learning frameworks
torch>=2.0.0
transformers>=4.36.0
accelerate>=0.25.0

# Training libraries
deepspeed>=0.12.0
trl>=0.23.0
peft>=0.7.0

# Data processing
datasets>=2.14.0
numpy>=1.24.0

# Optional: Evaluation and utilities
wandb  # For experiment tracking (optional)

# Note: Install PyTorch with CUDA support using:
# pip install torch --index-url https://download.pytorch.org/whl/cu121
